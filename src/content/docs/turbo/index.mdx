---
title: Turbo
description: AI Agent Orchestrator — terminal-based agent ecosystem that decomposes complex tasks, plans execution, and orchestrates autonomous agents via a squad of specialized LLM agents
---

import { Badge, Aside, Tabs, TabItem, Steps, FileTree, Card } from '@astrojs/starlight/components';

<Badge text="Alpha" variant="note" /> <Badge text="Python 3.14" variant="tip" />

Welcome to the **Turbo** documentation — an agent ecosystem orchestrator. Turbo provides a squad of specialized LLM agents that decompose complex tasks, plan execution, and deliver results autonomously — no human-in-the-loop.

---

## Overview

Turbo operates on a **TUI → Director → Agent Pipeline** model:

1. **TUI** (turbo-tui) — full-screen terminal with 4 modes (Director, Manager, Planner, Executor) + pipeline sidebar
2. **Director** (single point of contact) — persistent brain with memory, converses in pt-BR, refines scope, loads mode overlays (`/critic` or `/smart`)
3. **Manager** — decomposes complex tasks into discipline-based work packages (quality, security, docs, etc.)
4. **Planner** — creates structured execution plans with serial/parallel task optimization
5. **Executor** — executes plan tasks with git safety, retry logic, and atomic rollback
6. **Draw** — generates Unicode box-drawing diagrams from natural language descriptions

                     ┌──────────────┐
                     │    User      │
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  turbo TUI   │
                     │  ┌────────┐  │  ┌──────────────┐
                     │  │  Chat  │  │  │   Pipeline   │
                     │  │ Input  │  │  │   Sidebar    │
                     │  └────────┘  │  └──────────────┘
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │   director   │ ◀── sole contact point
                     └──────┬───────┘
                    ┌───────┼───────────┐
                    ▼       ▼           ▼
             ┌──────────┐ ┌──────┐ ┌──────────┐
             │  Manager │ │ draw │ │  critic  │
             │  Planner │ │ smart│ │ executor │
             │  Executor│ │  ... │ │  ...     │
             └─────┬────┘ └──────┘ └──────────┘
                   │         (modes)
                   ▼
             ┌──────────────┐
             │   pi agent   │
             ├──────────────┤
             │ 22 Skills    │
             │ 4 Extensions │
             └──────────────┘

## Philosophy

All agents operate under the **Autonomous Execution Mandate**:

- Agents do **not** assist — they **do**
- No human reads, reviews, or touches the output
- Submission is FINAL — no second chance, no handoff
- False completion is worse than failure
- Agents have no eyes or ears — verification is programmatic

## CLI Commands

| Command | Source | Description |
|---------|--------|-------------|
| `turbo` | `turbo.tui:main` | Full-screen TUI (Director, Manager, Planner, Executor modes) |
| `manager "prompt"` | `turbo.cli:manager_main` | Decompose a task into discipline-based work packages |
| `planner <demand-id>` | `turbo.cli:planner_main` | Create execution plan from a demand |
| `executor <plan-id>` | `turbo.cli:executor_main` | Execute a plan's tasks |
| `draw "prompt"` | `turbo.cli:draw_main` | Generate a Unicode box-drawing diagram |

## System Prompts

All agent instructions live in `agents/director/`:

| File | Role |
|------|------|
| `AGENTS.md` | Base director prompt (always active) |
| `modes/smart.md` | Intelligent/proactive overlay (`/smart`) |
| `modes/critic.md` | Critical/restrained overlay (`/critic`) |
| `modes/manager.md` | Task decomposition instructions |
| `modes/planner.md` | Execution plan instructions |
| `modes/executor.md` | Task execution instructions |
| `modes/draw.md` | Diagram generation instructions |

Each mode follows canonical sections: Autonomous Mandate (C0), Role, Principles, Constraints, Workflow, Output, Self-Verification, Anti-Patterns.

## Tech Stack

| Layer | Technology |
|-------|------------|
| Language | Python 3.14+ |
| CLI Framework | Typer |
| Terminal UI | `rich` library |
| AI Agent | pi coding agent (Node.js 22.12+) |
| Database | SQLite (WAL mode) via `turbo/db.py` |
| Testing | pytest + pytest-asyncio + pytest-mock |

## Quick Start

```bash
# TUI (recommended)
turbo

# Or CLI directly
manager "Refactor auth module to use JWT"
planner <demand-id>
executor <plan-id>
```
